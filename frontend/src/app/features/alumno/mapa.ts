/**
 * Re-export de UnidadMapa como Mapa.
 * El mapa general de unidades y el tablero interno Mario 3 ahora están unificados en
 * la interfaz arcade EDU-VISION con carrusel de cartuchos 3D y motor vertical-world
 * (unidad-mapa.ts).
 */
export { UnidadMapa as Mapa } from './unidad-mapa';
